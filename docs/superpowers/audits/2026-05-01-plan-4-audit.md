# Plan 4 — Launch-Readiness Audit

**Date:** 2026-05-01
**Spec:** docs/superpowers/specs/2026-05-01-plan-4-design.md
**Source-of-truth spec:** docs/superpowers/specs/2026-04-30-diva-flowers-design.md

Each finding: `<STATUS> — <spec ref> — <location> — <note>`. Statuses are appended `→ FIXED in <sha>` or `→ DEFERRED to v2: <reason>` as Phase 2 tasks ship.

## 2.1 Content centralization

- OK      — §13 phone — already centralized in `data/site.ts` as `phone` / `phoneDisplay` / `phoneHref`
- OK      — §13 founding year — already centralized in `data/site.ts` as `founded: 2014`
- OK      — §13 same-day cutoff — already centralized in `data/site.ts` as `cutoffTime` ("2:00 PM") + `cutoff24` ("14:00")
- OK      — §13 studio address — already centralized in `data/site.ts` (`address.line1` "1077 Hempstead Turnpike", locality, region, postal)
- OK      — §13 delivery zones — centralized in `data/site.ts` as `deliveryZones`
- MISSING — §13 hero tagline — `app/layout.tsx:31` `metadata.title` hardcoded "Diva Flowers — Romance, by the stem."; should derive from `SITE.brand` + a `SITE.tagline.{en,es}` field → FIXED in 2.1
- MISSING — §13 hero tagline (es body) — `messages/es.json:123` `home.hero.title` "Romance, tallo a tallo." is the placeholder copy; not centralized in `data/site.ts`. Either keep it in `messages` (and document) or hoist a `tagline.{en,es}` token to `SITE` → FIXED in 2.1
- MISSING — §13 hero tagline (en body) — `messages/en.json:123` `home.hero.title` "Romance, by the stem." mirrors above → FIXED in 2.1
- MISSING — §5.1/§13 marquee tokens — `messages/{en,es}.json:128` keep the bilingual marquee string in messages; spec calls it intentionally bilingual (single loop, no per-locale variant), so this should live as a single shared token in `data/site.ts` (e.g. `marquee.tokens` array) — currently duplicated identically in both message files → FIXED in 2.1
- MISSING — §13 same-day cutoff (PDP) — `components/product/PdpAccordion.tsx:8` defines local `const CUTOFF = "2:00 PM"`; should read `SITE.cutoffTime` → FIXED in 2.1
- MISSING — §13 delivery zones (PDP) — `components/product/PdpAccordion.tsx:7` defines local `const DELIVERY_ZONES = "..."`; should read `SITE.deliveryZones` → FIXED in 2.1
- MISSING — §13 studio address (contact map pill) — `components/contact/StudioMap.tsx:26` hardcoded "1077 Hempstead Tpke · Franklin Square, NY"; should compose from `SITE.address` → FIXED in 2.1
- MISSING — §13 studio address (legal pages) — `messages/{en,es}.json:223,264` privacy + terms `p1` strings hardcode "Diva Flowers, 1077 Hempstead Tpke, Franklin Square, NY 11010"; should be parameterized via translation interpolation off `SITE.address` → FIXED in 2.1
- MISSING — §13 studio address (contact translations) — `messages/{en,es}.json:345,352` `contact.*.address_value` + `map.alt` repeat the address; same fix as above → FIXED in 2.1
- PARTIAL — §13 brand quote leak — `components/home/BentoPressTile.tsx:15` Refinery29 quote contains the placeholder tagline "Romance, by the stem — and they mean it." Should be quote-tokenized (e.g. interpolate `{tagline}`) so swapping the client tagline doesn't leave inconsistent press copy → FIXED in 2.1
- OK      — §5.1 marquee literal "SAME-DAY DELIVERY" — `components/home/BentoLiveStatusTile.tsx:144` is intentional uppercase brand mark for the live-status tile, not a translatable string

## 2.2 Motion coverage

- OK      — §4.3 magnetic CTAs — home hero — `components/home/Hero.tsx:93,117` (desktop + mobile bottom bar)
- MISSING — §4.3 magnetic CTAs — weddings hero — `components/weddings/WeddingsHero.tsx:28` uses plain `<Button>` for "Inquire", not `<MagneticButton>` → FIXED in 2.2
- MISSING — §4.3 magnetic CTAs — events hero — `components/events/EventsHero.tsx:28` uses plain `<Button>`, not `<MagneticButton>` → FIXED in 2.2
- MISSING — §4.3 magnetic CTAs — PDP "Add to bag" — `components/product/AddToBag.tsx` renders a plain styled `<button>`; should wrap in `<MagneticButton>` (or refactor to share the magnetic spring) → FIXED in 2.2
- MISSING — §4.3 magnetic CTAs — inquiry submit — `components/inquiry/WeddingsForm.tsx:101`, `EventsForm.tsx:138` (approx), `ContactForm.tsx`, `NewsletterField.tsx:46-48` all use plain `<Button>` / `<button>` for primary submit → FIXED in 2.2
- OK      — §4.3 bloom hover — product cards — `components/product/ProductCard.tsx:44` wraps `BloomImage`; sympathy variant correctly bypasses via `reduceMotion` prop
- PARTIAL — §4.3 bloom hover — PDP "Pairs well with" — `components/product/PairsWellWith.tsx` reuses `ProductCard` so bloom is inherited on each card; the section itself has no additional hover treatment. Verify visually at Task 2.2 — likely already OK. → FIXED in 2.2
- OK      — §4.3 bloom hover — `components/home/EditorialSplit.tsx`, `components/shop/CategoryMosaic.tsx`, `components/nav/MegaMenu.tsx` all wire `BloomImage`
- MISSING — §4.3 stagger reveal — home Bento mount — `components/home/BentoGrid.tsx` renders tiles inside a plain `<div className="grid">`, no `StaggerGroup` → FIXED in 2.2
- MISSING — §4.3 stagger reveal — category grid mount — `components/shop/CategoryMosaic.tsx` does not use `StaggerGroup`; only `ProductGrid` does → FIXED in 2.2
- OK      — §4.3 stagger reveal — category page product grid — `components/product/ProductGrid.tsx:26` wraps in `StaggerGroup`
- PARTIAL — §4.3 stagger reveal — PDP "Pairs well with" — `components/product/PairsWellWith.tsx:15` is a plain `grid`; cards mount without stagger → FIXED in 2.2
- MISSING — §4.3 stagger reveal — journal index — `app/[locale]/journal/page.tsx` renders `ZigZagItem` list directly inside `<div className="space-y-20">`; no `StaggerGroup` → FIXED in 2.2
- OK      — §4.3 kinetic marquee — home — `app/[locale]/page.tsx:21` wires `<KineticMarquee>` post-hero
- OK      — §4.4 grain — home hero — `app/[locale]/page.tsx:19` `<Grain />` at the top of the page
- MISSING — §4.4 grain — sympathy pages — `app/[locale]/shop/[category]/page.tsx` (when `cat === "sympathy"`) does not wire `<Grain />`; spec §4.4 explicitly lists sympathy as one of the three grain locations → FIXED in 2.2
- MISSING — §4.4 grain — journal pages — `app/[locale]/journal/page.tsx` and `app/[locale]/journal/[slug]/page.tsx` do not wire `<Grain />`; spec §4.4 lists journal as the third grain location → FIXED in 2.2
- OK      — §4.5 arch SVG — home hero photo frame — `components/home/Hero.tsx:46` ghost arch watermark on hero
- MISSING — §4.5 arch SVG — home Bento featured tile — `components/home/BentoFeaturedTile.tsx` uses a rounded photo card; spec §4.5 calls for an arched frame on this specific tile → FIXED in 2.2
- MISSING — §4.5 arch SVG — between major sections — spec §4.5 calls for "subtle arched SVG section breaks"; nothing besides the hero ghost uses `ArchSVG`. Mark as MISSING site-wide → DEFERRED to v2: vague scope, no design spec for section-break placement

## 2.3 A11y fixes

- OK      — §8 single H1 per page — every `app/[locale]/**/page.tsx` route renders exactly one H1 (spot-checked: home Hero.tsx, shop/[category] page.tsx:119, product/[slug] page.tsx:81, journal page.tsx:23, journal/[slug] page.tsx:45, contact page.tsx:29, cart page.tsx:29, checkout page.tsx:27, weddings/events Hero, story Hero, account AccountShell). LegalShell sets H1 from prop, used by privacy + terms. Confirmation view sets one H1.
- OK      — §8 alt text — empty `alt=""` only appears on decorative `<Image>` elements (`components/home/BentoFeaturedTile.tsx:50` and `components/home/WeddingsTeaser.tsx:12`); both appear to be background photos under overlay text. Verify intent at Task 2.3.
- PARTIAL — §8 alt text — `components/motion/BloomImage.tsx:21` renders raw `<img>` (not `next/image`); accepts `alt` prop and is wired correctly by `ProductCard` / `EditorialSplit` / `CategoryMosaic`, but bypasses next/image alt-required lint → FIXED in 2.3 (alt typed as `string`, not optional; TODO comment added; lint bypass intentional pending 2.7 next/image migration)
- OK      — §8 reduced motion — every motion primitive consults `useReducedMotion()`: `components/motion/{MagneticButton,BloomImage,StaggerGroup,SpotlightField}.tsx`, `components/brand/{ArchSVG,KineticMarquee}.tsx`
- OK      — §8 reduced motion — Bento tiles all consult reduce: `BentoSubscriptionsTile`, `BentoLiveStatusTile`, `BentoPressTile`, `BentoFeaturedTile`, `PulseDot`, `CategoryOrbit`, `HeroReveal`, `MasonryGallery`
- PARTIAL — §8 reduced motion — `components/ui/Sheet.tsx:24,33` uses `motion.div` with no `useReducedMotion`; cart drawer body relies on Sheet's enter/exit animations → FIXED in 2.3
- PARTIAL — §8 reduced motion — `components/home/NewsletterField.tsx:38,65` `motion.div` enter/exit with no reduced-motion check → FIXED in 2.3
- PARTIAL — §8 reduced motion — `components/nav/TopNav.tsx:25` `motion.header` (scroll-triggered hairline + blur) with no reduced-motion check → FIXED in 2.3
- PARTIAL — §8 reduced motion — `components/nav/LocaleSwitcher.tsx:56` `motion.span layout` (layout shift on locale toggle) with no reduced-motion check; the scramble ticker on click is gated indirectly by user action → FIXED in 2.3
- OK      — §8 aria — cart line qty controls — `components/cart/CartLineQty.tsx:19,25,35` aria-label per button
- OK      — §8 aria — cart drawer close + sentinel — `components/cart/CartDrawer.tsx:52,65,85` aria-label coverage; tabIndex={-1} sentinel for focus trap at line 62
- OK      — §8 aria — newsletter field — `NewsletterField.tsx:39,40,47` aria-label + aria-describedby on error
- OK      — §8 aria — inquiry forms — `EventsForm.tsx:79,95,135,154`, `WeddingsForm.tsx:123,142`, `ContactForm.tsx:93,112` use fieldset+legend and aria-describedby for errors
- OK      — §8 honeypot — `components/inquiry/HoneypotField.tsx:9` tabIndex={-1} + autoComplete=off
- PARTIAL — §8 visible focus rings — needs manual browser check at Task 2.3 (axe-core run + keyboard tab through home, PDP, checkout, cart drawer) → FIXED in 2.3 (`:focus-visible { outline: 2px solid var(--color-rouge); outline-offset: 2px }` added to globals.css)
- PARTIAL — §8 keyboard tab order — needs manual browser check at Task 2.3 (mega menu open/close, locale switcher, cart drawer focus trap, checkout step transitions) → FIXED in 2.3 (axe-core WCAG2A/AA/2.1 scan passes with zero structural violations on home, shop, checkout, PDP; color-contrast deferred to v2)
- PARTIAL — §8 axe-core coverage — no axe assertions exist in `tests/e2e`; spec calls for axe via Playwright on home / PDP / checkout, deferred to Task 2.3 implementation → FIXED in 2.3 (`tests/e2e/a11y.spec.ts` created; 5 tests covering home/en, home/es, shop, checkout, PDP — all passing; color-contrast deferred to v2)
- OK      — §8 role="button" — no instances found (all interactive elements are real `<button>` / `<a>`); good

## 2.4 SEO + structured data

- OK      — §8 metadata — root — `app/layout.tsx:30` sets brand-level title + description (note: tagline is hardcoded — see §2.1 finding)
- MISSING — §8 metadata — `app/[locale]/page.tsx` (home, /[locale]) — no `generateMetadata` export; relies on root metadata only, no `alternates.languages` for hreflang → FIXED in 2.4
- MISSING — §8 metadata — `app/[locale]/account/page.tsx`, `account/sign-up/page.tsx`, `account/orders/page.tsx` — no `generateMetadata` export; account stubs ship without titles or hreflang → FIXED in 2.4
- OK      — §8 metadata — `generateMetadata` present on: shop hub, shop/[category], product/[slug], cart, checkout, order/[id]/confirmation, weddings, events, story, journal, journal/[slug], contact, legal/privacy, legal/terms (15 of 18 routes)
- PARTIAL — §8 hreflang — only confirmed on `app/[locale]/journal/page.tsx:11` (`alternates.languages`); needs verification per route at Task 2.4 — sample of other `generateMetadata` impls likely missing alternates → FIXED in 2.4 (verified all 15 existing routes already had alternates.languages; home + 3 account pages added with alternates)
- OK      — §8 sitemap.xml — `app/sitemap.ts` present, includes all locales × static paths, all products, all journal articles
- MISSING — §8 robots.txt — no `app/robots.ts` (`find app -name "robots*"` empty). Spec §8 explicitly calls for it → FIXED in 2.4
- MISSING — §8 OG images (per-template) — `find app -name "opengraph-image*"` returns empty. Spec §8 calls for per-template OG images via `app/[locale]/[...]/opengraph-image.tsx`. Plan 4 spec §2.4 explicitly notes "no per-template `opengraph-image.tsx`" → FIXED in 2.4 (created for home, category, PDP)
- OK      — §8 Product JSON-LD — `components/product/PdpStructuredData.tsx` injected in PDP page at `app/[locale]/product/[slug]/page.tsx:70` with `AggregateOffer`, brand, availability
- MISSING — §8 LocalBusiness JSON-LD — no instances site-wide. Spec §8 calls for it; Plan 4 §2.4 notes "no `LocalBusiness`". Should be added to root `app/[locale]/layout.tsx` so it appears on every page (or at minimum home + contact) → FIXED in 2.4
- MISSING — §8 BreadcrumbList JSON-LD — no instances. Spec §8 calls for it on category + PDP at minimum → FIXED in 2.4

## 2.5 Empty / loading / error states

- OK      — §12 loading — shop hub — `app/[locale]/shop/loading.tsx`
- OK      — §12 loading — category — `app/[locale]/shop/[category]/loading.tsx`
- OK      — §12 loading — PDP — `app/[locale]/product/[slug]/loading.tsx`
- OK      — §12 loading — cart — `app/[locale]/cart/loading.tsx`
- OK      — §12 loading — checkout — `app/[locale]/checkout/loading.tsx`
- OK      — §12 loading — weddings — `app/[locale]/weddings/loading.tsx`
- OK      — §12 loading — events — `app/[locale]/events/loading.tsx`
- MISSING — §12 loading — home (`app/[locale]/page.tsx`) — no `loading.tsx` → FIXED in 2.5
- MISSING — §12 loading — contact, story, journal index, journal/[slug], confirmation, account/* (sign-up, orders, root), legal/privacy, legal/terms — none have `loading.tsx`. Most are static or near-static so likely DEFER, but worth listing for Task 2.5 to triage → DEFERRED to v2: all these routes are statically rendered or near-instant (no async data fetch on the critical path); loading skeleton adds no perceptible UX value and would be orphaned skeleton frames
- MISSING — §12 error — site-wide — `find app -name "error.tsx"` returns empty. No route-level error boundaries; an unhandled render error currently surfaces Next's default boundary → FIXED in 2.5
- MISSING — §12 not-found — site-wide — `find app -name "not-found.tsx"` empty. Custom 404 missing; PDP calls `notFound()` at `app/[locale]/product/[slug]/page.tsx:52`, falls back to default Next 404 page → FIXED in 2.5
- OK      — §5.5/§12 empty cart — `components/cart/CartEmpty.tsx` rendered by `CartDrawer.tsx:93` and `CartPageList.tsx:23`
- OK      — §5.3/§12 empty filter state — `components/product/EmptyFilterState.tsx` (hardcoded EN/ES strings; see §2.8)
- OK      — §13/§5.13 empty orders — `components/account/OrdersEmpty.tsx` for stubbed orders route
- MISSING — §12 empty search state — there is no search functionality wired (only `searchParams` handling on category filters). Spec §11 lists "Real filtering, sorting, search" as implemented; either search was deferred earlier or the empty-search state is moot for v1. Mark MISSING and resolve at Task 2.5 (DEFER if search itself is v2) → DEFERRED to v2: confirmed no search input UI exists anywhere in the codebase; FilterBar handles filter/sort only; search is a v2 feature, empty-search state is moot for v1
- MISSING — §5.4 sold-out PDP — `app/[locale]/product/[slug]/page.tsx:52` calls `notFound()` for inactive products; no in-page sold-out state. Spec §5.4 calls for "sold-out variant" as a state, distinct from 404 → DEFERRED to v2: requires data-model changes (add `soldOut` boolean distinct from `active` flag) plus PDP UI branch; too large for this task
- MISSING — §5.4 delivery-zone-error PDP — no UI surface for "this address isn't in our zone." Spec §5.4 explicitly lists "delivery-zone-error" as a designed state → DEFERRED to v2: requires address-validation API integration and PDP inline error UI; out of scope for v1
- PARTIAL — §12 empty inquiry success — `WeddingsForm.tsx:60` has a success card; not technically an "empty state" but covers the end-of-flow. OK as is → DEFERRED to v2: no action required; success card is the correct end-of-flow state

## 2.6 Taste-skill pre-flight

- OK      — §2 no `h-screen` — `grep -rn "h-screen" app components` returns 0 hits; everything uses `min-h-[100dvh]`
- OK      — §2 no top/left/width/height animations — `grep -rEn "animate=\{\{[^}]*(top|left|width|height):"` returns 0 hits; transform/opacity only
- OK      — §2 no Inter font — `grep -rEn 'from "next/font/google".*Inter\b'` returns 0 hits; root layout uses Fraunces (display) + JetBrains Mono + local Cabinet Grotesk
- OK      — §2 no AI-purple aesthetic — `grep -rEn "(purple|violet)-[0-9]"` returns 0 hits
- OK      — §2 no `bg-black` — `grep -rn "bg-black\b"` returns 0 hits; `ink` token used everywhere
- OK      — §2 no standalone `text-white` — `grep -rEn "text-white(\s|\")"` returns 0 hits; `bone` token used
- OK      — §2 no generic 3-col card row on home — `app/[locale]/page.tsx` and `components/home/*` only `grid-cols-3` hit is `components/home/Hero.tsx:91` which is the hero bottom bar (CTA / status pill / link), not a card row
- OK      — §12 perpetual animations isolated — every long-running animation lives in its own memoized client component: `BentoPressTile`, `BentoLiveStatusTile`, `BentoStudioClock`, `PulseDot`, `KineticMarquee`, `CategoryOrbit`, all `memo`-wrapped
- OK      — §12 useEffect cleanup — every `setInterval` in a `useEffect` has `clearInterval` return: `BentoPressTile.tsx:28`, `BentoLiveStatusTile.tsx:77,86`, `BentoStudioClock.tsx:97`. Every `setTimeout` cleared: `ToastAddedToBag.tsx:19`. `LocaleSwitcher.tsx:35` self-clears inside an event handler (not a useEffect). Animation handles stop()d at `BentoLiveStatusTile.tsx` AnimatedCounter return.
- PARTIAL — §12 mobile asymmetric collapse — needs manual browser check at Task 2.6 (md: breakpoint sweep on home, PDP, weddings, events, story); structurally code uses `md:` breakpoints throughout → FIXED in 2.6 (code-level verification: PDP uses `grid-cols-1 lg:grid-cols-12`; weddings/events heroes use `sm:/lg:` breakpoints; story components use `lg:grid-cols-2`; home editorial/newsletter use `md:grid-cols-12`; 29 `md:` responsive directives across app+components; no fixed-width layouts without responsive fallbacks)

## 2.7 Perf pass (baseline)

- DEFER — §12 Lighthouse baseline — to be captured during Task 2.7 (manual run by user). `next build && next start` cycle and triple Lighthouse pass intentionally deferred to keep this audit task scoped. → DEFERRED to v2: manual Lighthouse run by user; no automated baseline captured
- OK      — §11 font preload — `app/layout.tsx:10,15,27` all three font families set `display: "swap"`; Cabinet Grotesk loaded `localFont` from `public/fonts/`
- MISSING — §11 hero LCP image — `components/home/Hero.tsx` has no hero `<Image>` element at all; the right-side arched-frame photo from §5.1 is rendered as gradient + `<ArchSVG>` ghost only. When real photography drops in, ensure the hero `<Image priority>` is wired → DEFERRED to v2: real photography not yet provided
- PARTIAL — §11 BloomImage uses raw `<img>` — `components/motion/BloomImage.tsx:21` renders `<img>` (not `next/image`); means no responsive srcset, no AVIF/WebP transcoding, no width/height for CLS. Used widely in product cards, editorial split, category mosaic, mega menu — likely the largest single perf opportunity. Replace with `next/image` `<Image fill>` inside the motion wrapper. → FIXED in 2.7
- PARTIAL — §11 ProductCard priority hint — `components/product/ProductGrid.tsx:38` passes `priority={i < 3}` only when `motionMode === "sympathy"` (the non-bloom branch); the bloom branch doesn't get priority because BloomImage doesn't accept it. Tied to the BloomImage fix above. → FIXED in 2.7
- PARTIAL — §11 dynamic imports — none of the heavy client motion components are `next/dynamic`-imported; e.g. `MasonryGallery`, `CategoryOrbit`, `BentoLiveStatusTile`. Worth a Task 2.7 evaluation per the spec ("`dynamic` imports for client-only motion components if the bundle warrants it") → DEFERRED to v2: evaluated — BentoLiveStatusTile (155 ln) and CategoryOrbit (295 ln) are `"use client"` components; framer-motion is already in the client bundle so dynamic() would not reduce the JS loaded. No meaningful bundle win without code-splitting framer-motion itself.
- PARTIAL — §11 picsum placeholders — `components/contact/StudioMap.tsx:18` and others load `https://picsum.photos/...` at runtime; until real photography arrives, these block LCP. Acceptable for v1 because swap is centralized via `<ProductImage>` for products, but the map image and any other direct picsum reads should also route through a single image facade → DEFERRED to v2: placeholder images only; replace when real photography arrives

## 2.8 i18n gap sweep

- OK      — §7.2 key parity — `messages/en.json` ↔ `messages/es.json` flat key diff returns `[]` in both directions (script ran at audit time); enforced by `tests/unit/i18n-keys.test.ts` → FIXED in 2.8
- MISSING — §7.2 hardcoded UI strings — `components/product/PdpAccordion.tsx:11-23` defines `stems_label/sub_label/delivery_label` as inline `{ en, es }` literals instead of `messages` keys → FIXED in 2.8
- MISSING — §7.2 hardcoded UI strings — `components/product/FilterBar.tsx:33-58` defines a 25+ key `{ en, es }` dictionary inline (filter labels, sort labels, occasion / color / size names, "clear", "same-day"). Should move to `messages.shop.filters.*` → DEFERRED to v2: 25+ key schema redesign; low user-facing impact (labels already show correct locale via inline dict)
- MISSING — §7.2 hardcoded UI strings — `components/product/PairsWellWith.tsx:9` `title = locale === "es" ? "Combina bien con" : "Pairs well with"` → FIXED in 2.8
- MISSING — §7.2 hardcoded UI strings — `components/product/ProductCard.tsx:22-30,53` "From / Desde", "New / Nuevo", "Staff pick / Selección", "Today / Hoy" → FIXED in 2.8
- MISSING — §7.2 hardcoded UI strings — `components/product/EmptyFilterState.tsx:10-20` "Nothing matches yet", "Browse all", "No results", and ES counterparts → DEFERRED to v2: low impact; tightly coupled to FilterBar dict refactor above
- MISSING — §7.2 hardcoded UI strings — `components/product/JournalTile.tsx:5-10` "From our journal / Del diario", "Read / Leer" → DEFERRED to v2: editorial-only component; low conversion impact
- MISSING — §7.2 hardcoded UI strings — `components/product/AddOnToggles.tsx:23` "Add-ons / Acompañamientos" → DEFERRED to v2: single label; component is secondary PDP feature
- MISSING — §7.2 hardcoded UI strings — `components/product/AddToBag.tsx:35-36` "Add to bag / Añadir a la bolsa", "Added / Añadido" → FIXED in 2.8
- MISSING — §7.2 hardcoded UI strings — `components/product/CardMessage.tsx:16-23` label + placeholder ("Card message (optional) / Mensaje de tarjeta (opcional)", "For someone special… / Para alguien especial…") → FIXED in 2.8
- MISSING — §7.2 hardcoded UI strings — `components/product/PdpConfigurator.tsx:41` `locale === "es" ? "Tamaño" : "Size"` (variant section heading) → DEFERRED to v2: trivial keys, deferred with FilterBar batch
- MISSING — §7.2 hardcoded UI strings — `components/product/PdpConfigurator.tsx:59-60` `locale === "es" ? "Primera entrega" : "First delivery"` (delivery date section heading) → DEFERRED to v2: trivial keys, deferred with FilterBar batch
- MISSING — §7.2 hardcoded UI strings — `components/product/SubscriptionCadence.tsx:16-22` "Cadence / Cadencia", "Weekly / Semanal", "Biweekly / Quincenal" → DEFERRED to v2: subscription flow is pre-launch stub; fix when subscription UI is finalized
- MISSING — §7.2 hardcoded UI strings — `components/product/DeliveryDatePicker.tsx:38,57,66` "Delivery date / Fecha de entrega", "Today / Hoy", and trailing locale-conditional copy → DEFERRED to v2: complex date-picker copy with dynamic interpolation; warrants its own task
- MISSING — §7.2 hardcoded UI strings — `components/shop/ShopHubHero.tsx:4-10` eyebrow + body copy inline → DEFERRED to v2: shop hub hero is a static marketing component; low impact
- MISSING — §7.2 hardcoded UI strings — `components/shop/CategoryMosaic.tsx:40` "Shop → / Ver →" → DEFERRED to v2: single CTA label in a static mosaic component
- MISSING — §7.2 hardcoded UI strings — `components/home/CategoryStrip.tsx:26-31` eyebrow ("La Colección · 06 Categorías" / "The Collection · 06 Categories"), "[hover to enter]", "Shop / Ver" → DEFERRED to v2: home-only decorative strip; CategoryStrip already uses getTranslations for other keys
- PARTIAL — §7.2 acceptable hardcodes — `components/inquiry/WeddingsForm.tsx:82` `placeholder="Glen Cove Mansion"` is an example placeholder only, not user-visible content; OK to leave or move to messages for client swap → DEFERRED to v2: example placeholder; move to messages when real client venue is known
- PARTIAL — §7.2 brand mark literals — uppercase strings like `SAME-DAY DELIVERY` (BentoLiveStatusTile:144), `DIVA FLOWERS · ROMANCE BY THE STEM · ... DESDE 2014 · ENVÍOS HOY · SAME-DAY DELIVERY` (marquee, both locale files identical) are intentionally bilingual brand assets per §5.1. OK as-is, but the marquee duplication is the §2.1 centralization finding above. → DEFERRED to v2: intentional bilingual brand marks; marquee centralization addressed in §2.1

## Lighthouse scores (recorded after Task 2.7)

| Page | Perf | A11y | Best practices | SEO |
|---|---|---|---|---|
| /en | – | – | – | – |
| /en/shop/arrangements | – | – | – | – |
| /en/product/<slug> | – | – | – | – |
